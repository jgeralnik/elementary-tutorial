version: 2

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

          - unique:
              config:
                tags:
                  - GPT
              meta:
                owner:
                  - joey.g@elementary-data.com
                description: Check unique with GPT
          - unique:
              config:
                tags:
                  - GPT
              meta:
                description: This is a primary key so should be unique
          - not_null:
              config:
                tags:
                  - GPT
              meta:
                description: This is a primary key so should be unique
  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
          - unique:
              config:
                tags:
                  - GPT
              meta:
                description: This is a primary key so should be unique
          - not_null:
              config:
                tags:
                  - GPT
              meta:
                description: This is a primary key so should be unique
